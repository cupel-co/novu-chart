{{- if .Values.ws.httpRoute.enabled -}}
{{- $parentName := default (include "novu.fullname" .) .Values.ws.httpRoute.parentRef.name -}}
{{- $parentNamespace := default .Release.Namespace .Values.ws.httpRoute.parentRef.namespace -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "novu.ws.fullname" . }}
  labels:
    {{- include "novu.labels" . | nindent 4 }}
    app.kubernetes.io/component: ws
  {{- with .Values.ws.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    - name: {{ $parentName }}
      namespace: {{ $parentNamespace }}
      {{- if .Values.ws.httpRoute.parentRef.sectionName }}
      sectionName: {{ .Values.ws.httpRoute.parentRef.sectionName | quote }}
      {{- end }}
  {{- if .Values.ws.httpRoute.hostname }}
  hostnames:
    - {{ .Values.ws.httpRoute.hostname | quote }}
  {{- end }}
  rules:
    - backendRefs:
        - name: {{ include "novu.ws.fullname" . }}
          port: {{ .Values.ws.port }}
{{- end }}
